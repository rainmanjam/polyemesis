{
  "name": "polyemesis-web",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/check-build.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/markdown-satteri": "0.4.1",
    "@astrojs/sitemap": "^3.7.4",
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^7.3.2",
    "mermaid": "^12.0.0",
    "motion": "^13.2.0",
    "tailwindcss": "^4.1.18",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.1",
    "jsdom": "^30.1.0",
    "vitest": "^5.0.0",
    "wrangler": "4.135.0"
  },
  "//overrides": "lodash-es is pulled in by chevrotain, which mermaid 12 added. Versions up to 4.17.23 carry GHSA-r5fr-rjxr-66jc (code injection via _.template) and GHSA-f23m-r3pf-42rh (prototype pollution in _.unset/_.omit); 4.18.1 is clean. chevrotain has not moved, and npm audit fix would downgrade mermaid back to 11 rather than fix the dependency. The override DEDUPES -- one lodash-es at 4.18.1, no nested copy -- so nothing is shadowed. REMOVE THIS once chevrotain ships a release depending on >=4.18.1.",
  "overrides": {
    "lodash-es": "^4.18.1"
  }
}
